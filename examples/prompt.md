Create a figure with subplots organized as:

| a        |  b |
| c | d |    e  |

Note that some of the plots span over others. The subplots should show:

- a) an imshow with an inset scatter
- b) a network
- c)  a scatter plot
- d)  a geoplot with a contour  across Australia
- e)  a grouped bar chart

Data can be made up and should show "some complexity". The formatting should have abc title formatted as "(a)". Titles should be descriptive and centered. Colorbars should be present if necessary with clear titles.
